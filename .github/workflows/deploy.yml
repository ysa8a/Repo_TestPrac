name: Deploy to Render

on:
  push:
    branches:
      - Vulnerabilidad  # Cambia si usas otro nombre de rama

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy Hook
        run: |
          curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
